using UnityEngine;
using System.Collections;

public class SpriteRenderLayer : MonoBehaviour {
    public string layer = "Foreground";
    // Use this for initialization
    void Start () {
        ParticleSystem sr = GetComponent<ParticleSystem>();
        
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
